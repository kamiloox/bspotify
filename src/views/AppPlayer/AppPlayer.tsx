import MainTemplate from '../../components/templates/MainTemplate/MainTemplate';

const AppPlayer = () => {
  return (
    <MainTemplate>
      <p>hello</p>
    </MainTemplate>
  );
};

export default AppPlayer;
